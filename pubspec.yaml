name: flutter_switch_device
description: A Flutter widget that renders network switch devices with ports, fully programmatic — no SVG assets required.
version: 0.2.0
homepage: https://github.com/lianghualin/flutter_switch_device
repository: https://github.com/lianghualin/flutter_switch_device
issue_tracker: https://github.com/lianghualin/flutter_switch_device/issues
topics:
  - widget
  - network
  - switch
  - visualization

environment:
  sdk: ^3.11.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
